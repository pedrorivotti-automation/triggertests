properties([
    $class: 'GithubProjectProperty',
    displayName: '',
    projectUrlStr: 'https://github.com/enthought/buildsystem/'
    ],
	[pipelineTriggers([
			pollSCM(
			    ignorePostCommitHooks: true,
			    scmpoll_spec: 'H/3 * * * *'
			)]
	])

print("Hello")
